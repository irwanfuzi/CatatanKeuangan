name: Build & Deploy Flutter PWA Web

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter (Lock 3.24.3)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'
          channel: 'stable'

      - name: Install Dependencies
        run: flutter pub get

      # 1. KOMPILASI FLUTTER WEB
      - name: Build Flutter Web
        run: flutter build web --release --base-href "/" --no-tree-shake-icons

      # 2. COMMIT & PUSH ARTIFACT BUILD KE MAIN BRANCH
      - name: Commit & Push build/web ke Main
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add build/web -f
          git commit -m "chore: auto-update web build artifacts" || echo "No changes to commit"
          git push origin main || echo "No changes to push"

      # 3. UPLOAD UNTUK GITHUB PAGES
      - name: Upload Web Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy-web:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
